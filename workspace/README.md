# PAIOS Workspace

Drop files here so the agent can browse and read them via `workspace_list` and `workspace_read` tools.

Supported for text/code files directly. For images, video, and PDF — attach them in chat (📎 button).
